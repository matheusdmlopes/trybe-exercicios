const paiDeTodos = document.getElementById("paiDoPai")

const todosOsFilhos = paiDeTodos.childNodes


for (let index = 0; index < paiDeTodos.length; index += 1) {
    let element = paiDeTodos[index]

    if (element.includes(""))
}
